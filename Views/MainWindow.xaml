<Window 
    x:Class="WPF_Desafio.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:entities="clr-namespace:Incident.Domain.Entities;assembly=Incident.Domain"
    xmlns:local="clr-namespace:WPF_Desafio" 
    xmlns:viewmodels="clr-namespace:Incident.WPF.ViewModels" 
    d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
    mc:Ignorable="d"
    Title="Intern Incidents" WindowState="Maximized"
>
    <Window.Resources>
        <!-- Collections -->
        <CollectionViewSource x:Key="ticketsViewSource" Source="{Binding Tickets}"/>
        <CollectionViewSource x:Key="commentsViewSource" Source="{Binding Comments, Source={StaticResource ticketsViewSource}}"/>
        <CollectionViewSource x:Key="authorsViewSource" Source="{Binding Authors}"/>
        
        <!-- Enums -->
        <ObjectDataProvider x:Key="ticketStateEnum" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="entities:TicketState"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>
        
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título de Layout -->
        <TextBlock Grid.Row="0" Text="Incidencias internas" Margin="0,0,0,10" Style="{StaticResource TitleTextBlockStyle}" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
        
        <!-- Búsqueda y filtro de Tickets -->
        <Label Grid.Row="1" Content="Buscar Tickets:" Margin="0,0,0,5" Foreground="{DynamicResource TextFillColorPrimaryBrush}" /> 
        <TextBox Grid.Row="2" x:Name="ticketsSearch" Margin="0,0,0,10" Height="25" VerticalContentAlignment="Center">
            <TextBox.Text >
                <Binding Path="Search.Text" UpdateSourceTrigger="PropertyChanged" Delay="500" />
            </TextBox.Text>
        </TextBox>

        <!-- Grilla de datos de Tickets -->
        <Label Grid.Row="3" Content="Tickets:" Margin="0,0,0,5" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
        <DataGrid Grid.Row="4" x:Name="ticketsDataGrid" AutoGenerateColumns="False" 
          EnableRowVirtualization="True" VerticalAlignment="Top"
          ItemsSource="{Binding Source={StaticResource ticketsViewSource}}" 
          Margin="0,0,0,5" RowDetailsVisibilityMode="VisibleWhenSelected">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Estado" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding State}" VerticalAlignment="Center" Margin="4,0" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <ComboBox SelectedItem="{Binding State, UpdateSourceTrigger=PropertyChanged}" 
                                      ItemsSource="{Binding Source={StaticResource ticketStateEnum}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
                
                <DataGridTextColumn Binding="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Header="Título" Width="*"/>
                <DataGridTextColumn Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Header="Descripción" Width="2*"/>

                <DataGridTemplateColumn Header="Autor" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Author.Name}" VerticalAlignment="Center" Margin="4,0" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <ComboBox SelectedItem="{Binding Author, UpdateSourceTrigger=PropertyChanged}" 
                                      DisplayMemberPath="Name"
                                      ItemsSource="{Binding Source={StaticResource authorsViewSource}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Controles de Paginación -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Button x:Name="btnPrevPage" Content="&lt; Anterior" Command="{Binding Paginator.PrevPageCommand}" Width="75" Margin="0,0,10,0" Padding="5,2"/>
            <TextBlock x:Name="txtPageInfo" Text="{Binding Paginator.PageInfo}" VerticalAlignment="Center" FontWeight="Medium"/>
            <Button x:Name="btnNextPage" Content="Siguiente &gt;" Command="{Binding Paginator.NextPageCommand}" Width="75" Margin="10,0,0,0" Padding="5,2"/>
        </StackPanel>

        <!-- Grilla de datos de Comentarios -->
        <Label Grid.Row="6" Content="Comentarios:" Margin="0,0,0,5" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
        <DataGrid Grid.Row="7" x:Name="commentsDataGrid" AutoGenerateColumns="False" 
          EnableRowVirtualization="True" VerticalAlignment="Top"
          ItemsSource="{Binding Source={StaticResource commentsViewSource}}" 
          Margin="0,0,0,10" RowDetailsVisibilityMode="VisibleWhenSelected">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Content, UpdateSourceTrigger=PropertyChanged}" Header="Contenido" Width="*"/>
                <DataGridTemplateColumn Header="Autor" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Author.Name}" VerticalAlignment="Center" Margin="4,0" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <ComboBox SelectedItem="{Binding Author, UpdateSourceTrigger=PropertyChanged}" 
                                      DisplayMemberPath="Name"
                                      ItemsSource="{Binding Source={StaticResource authorsViewSource}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Binding="{Binding CreatedAt, StringFormat=\{0:g\}}" Header="Creada el" Width="Auto" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="8" Width="100" HorizontalAlignment="Right" Content="Save" Command="{Binding SaveCommand}" />
    </Grid>
</Window>
